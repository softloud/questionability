# Canonical location of dbt profiles: ~/.dbt/profiles.yml (system-wide default)
name: qn_project
version: "1.0.0"
config-version: 2

profile: qn_project

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  qn_project:
    +schema: seeds

models:
  analyses:
    +tags: ['dbt analyses']
  qn_project:
    source_entities:
      +materialized: view
      +schema: source_entities
      +tags: ['source_entities']
    semantic:
      +materialized: ephemeral
      +tags: ['semantic']
    analytic:
      +materialized: table
      +schema: analytic
      +tags: ['analytic']
    dbt_artifacts:
      +materialized: table
      +schema: artifacts
      +tags: ['dbt artifacts']

